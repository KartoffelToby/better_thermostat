{
  "1fbab97df0f62da4273afbaca133a4f146ab4ed7": "The same fix exists on both lines in different shapes. The development line owns its background tasks through _spawn_owned/_owned_tasks in climate.py; the maintenance-line commit wraps the same mechanism in handler docstrings and startup tests whose surroundings the development line's structure does not share, so those markers find no textual match.",
  "84a06b8cf763986bd8b7a08fe707ffd741b3f3e2": "The same fix exists on both lines in different shapes. The maintenance line's MQTT adapter reads the offset bounds itself, so every read there grew its own guard against a TRV that has no calibration entity. On the development line those reads delegate to the generic adapter, where a single _calibration_state carries the guard for every entity-backed ecosystem. The guard's own lines are in the development tree; the markers that find no match are the maintenance line's fixture and its three test names, which the development line covers in one class parametrized over the generic, MQTT and Z-Wave JS adapters."
}
